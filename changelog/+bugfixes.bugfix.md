Fix a batch of bugs found while raising test coverage: duplicated rows in one export were uploaded with the same import id so YNAB kept only one; amounts were truncated instead of rounded to milliunits; the checkpoint recorded the earliest processed date instead of the latest; `--reconcile` did not apply to new transactions; rejecting a partial match against a reconciled transaction created it as reconciled; very short payees matched unrelated YNAB payees; the resolved YNAB payee was never sent on upload; `yul load -a` crashed for an entity without a config entry; an empty export crashed; BBVA and Cobee could not read amounts over a thousand and BBVA crashed on a blank PDF cell; a Cobee cancellation marker could drop the wrong transaction; an unsupported input file ended in a traceback instead of an error message; the budget list was not shown during setup; a partial match without a payee rendered `None`; amount colours were dropped for symbol-first currencies; reconciling with nothing selected crashed; the reconcile confirmation read "1 transactions".
